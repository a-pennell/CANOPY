# Canopy Architecture

This directory holds implementation architecture notes that govern the Canopy build.

The source planning artifacts live in `outputs/`. The master build artifact is:

- `outputs/canopy_build_blueprint.md`

Implementation decisions should preserve the Canopy-first rule: CommonCredit, ICOS,
Sensemaking, and Stewardship are folded capability ancestry, not separate apps.
